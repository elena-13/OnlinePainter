import React from 'react';
import './canvas.scss';

const Canvas = () => {
  return (
    <div className="canvas">
      <canvas width={600} height={600}></canvas>
    </div>
  );
};

export default Canvas;
